apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: cezzis-com-cocktails-api-updater
  namespace: argocd
spec:
  applicationRefs:
    - namePattern: "cezzis-com-cocktails-api"
      commonUpdateSettings:
        updateStrategy: "semver"
        forceUpdate: true
      images:
        - alias: "cezziscomcocktailsapi"
          imageName: "acrveceusgloshared001.azurecr.io/cezziscomcocktailsapi:0.x"
          manifestTargets:
            kustomize:
              name: "acrveceusgloshared001.azurecr.io/cezziscomcocktailsapi"
  writeBackConfig:
    method: "git:secret:argocd/git-creds"
    gitConfig:
      branch: main
      writeBackTarget: kustomization
